This program uses a stack interface to implement two versions of a stack and performs arithemetic operations, mimicing what the program stack does. The two implementations are linked and array based.
